---
# defaults file for ansible-role-vault
vault_version: "0.8.3"
vault_root: /opt/vault
vault_install_dir: "{{vault_root}}/{{vault_version}}"
vault_bin_dir: /opt/bin
vault_profile_path:
  Darwin:
    /usr/local/etc/profile.d
  Debian:
    /etc/profile.d
  RedHat:
    /etc/profile.d
